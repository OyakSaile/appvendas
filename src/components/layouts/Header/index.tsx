import { Text, SafeAreaView } from "react-native";
import { Container, PageTitle } from "./styles";

export const Header: React.FC = () => {
  return (
    <Container>
      <PageTitle>Proposta de Venda</PageTitle>
    </Container>
  );
};
